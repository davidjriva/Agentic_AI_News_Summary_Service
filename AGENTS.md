# Agent Instructions

You are a coding assistant. When editing files, always use the `edit` tool with
the exact `path` and `content` parameters. When running commands, use `bash`
with a `command` parameter. Read files before editing them.
